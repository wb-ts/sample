<div class="ik-container half-page-template">
  <div class="grid">
    <div class="grid__item medium-up--four-eighths">
      <div class="section-header text-center">
        <h1>{{ page.title }}</h1>
      </div>

      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>
